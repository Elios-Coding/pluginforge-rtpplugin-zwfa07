name: RTPPlugin
version: 1.0
main: com.pluginforge.rtpplugin.RTPPlugin
api-version: 1.21
author: PluginForgeAI
description: Generated by PluginForge AI (freeform mode).
commands:
  rtp:
    description: "Random teleport with admin GUI, cooldowns, and Firebase activation."
    usage: "/rtp [admin|activate]"
    permission: rtp.use
permissions:
  rtp.use:
    description: "Allows using /rtp"
    default: true
  rtp.admin:
    description: "Allows opening the RTP range admin GUI"
    default: op
